package com.didit.app.ui.theme

import androidx.compose.ui.graphics.Color

// Mirrors the CSS custom properties in src/styles/globals.css (now retired).
object DiditColors {
    val Brand = Color(0xFF5A189A)
    val BrandForeground = Color(0xFFFFFFFF)
    val Accent = Color(0xFFFFC300)
    val AccentForeground = Color(0xFF000000)

    val Background = Color(0xFFF2F2F2)
    val Foreground = Color(0xFF000000)
    val Card = Color(0xFFFFFFFF)
    val CardForeground = Color(0xFF000000)
    val Muted = Color(0xFFC2C2C2)
    val MutedForeground = Color(0xFF4D4D4D)
    val Border = Color(0xFFD9D9D9)

    val Destructive = Color(0xFFDF2020)
    val DestructiveForeground = Color(0xFFFFFFFF)
}
