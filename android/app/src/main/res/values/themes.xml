<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Minimal manifest theme — Compose (DiditTheme) does the real styling. Light-only, matching the app. -->
    <style name="Theme.didit" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#F2F2F2</item>
    </style>
</resources>
